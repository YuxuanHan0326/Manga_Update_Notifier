# Worklog

## Archive Summary
- Archive file: worklog_archive/WORKLOG_ARCHIVE_20260311_061924.md
- Reason: active WORKLOG.md exceeded 150 lines and was rotated per protocol.
- Key outcomes carried forward:
  - Completed subscription cover bugfix for CopyManga + KXO.
  - Added on-read backfill for historical subscriptions missing `item_meta.cover`.
  - Added regression tests (unit + integration) and validation passed.
  - Docker runtime verification passed (`compose up`, container `Up`, `/api/health=200`).

## 2026-03-11

### 01. Worklog Rotation
- Rotated oversized WORKLOG (>150 lines) into archive.
- Recreated concise WORKLOG with handoff summary.
### 02. Final Validation Snapshot
- Backend lint/tests passed (`ruff`, `pytest unit=23`, `pytest integration=10`).
- Frontend checks passed (`npm run lint`, `npm run test`, `npm run build`; test/build required elevated mode due sandbox `spawn EPERM`).
- Runtime verified: `docker compose ps` is `Up`, `/api/health` returned `200`.
### 03. Bugfix Round Start: Cover Render + Schedule Tick Alignment
- Marked `T-047` as `IN_PROGRESS` and updated `NEXT_STEP.md`.
- Planned to fix two user-facing issues in one minimal round: subscription cover rendering failure and schedule toggle tick alignment.
### 04. Bugfix Round: Cover Render + Schedule Toggle Alignment
- Diagnosis:
  - `/api/subscriptions` returned valid cover URLs, but browser-side direct fetch failed under source hotlink/host policy.
- Backend change:
  - Added `GET /api/cover-proxy` in `app/api.py` with explicit allowed host suffix checks to avoid open-proxy behavior.
- Frontend change:
  - Updated `frontend/src/main.js` to render cover images through `/api/cover-proxy` and keep `No Cover` fallback on load failure.
  - Updated `frontend/src/main.js` + `frontend/src/style.css` to align schedule toggle checkbox next to label text.
- Validation:
  - `ruff` passed.
  - `pytest unit` passed (23).
  - `pytest integration` passed (12).
  - `npm run lint/test/build` passed (test/build required elevated mode due esbuild `spawn EPERM` in sandbox).
  - Docker runtime verification passed (`compose up -d --build`, `compose ps`, `/api/health`).
- Runtime smoke:
  - `/api/cover-proxy` returned `200 image/jpeg` for tested cover URLs and image rendering recovered.
### 05. Bugfix Round Start: KXO Cover Extraction Debug
- Set `T-048` to IN_PROGRESS and updated `NEXT_STEP.md` for KXO cover diagnostic scope.
- Plan: inspect live KXO snapshot output, confirm cover URL format/reachability, then apply minimal parser fix if needed.
### 06. KXO Cover Extraction Debug and Fix
- Diagnostic result:
  - KXO `item_meta.cover` exists (adapter extraction path works).
  - Failure point is cover proxy fetch policy for `kmimg.mxomo.com`: previous referer strategy produced upstream `403`.
- Implementation:
  - `app/api.py` cover proxy now uses host-specific referer fallback for `mxomo` hosts (`None -> https://kzo.moe/ -> https://kxo.moe/`).
  - Added helper `_cover_referer_candidates` and kept allowlist boundary unchanged.
- Validation:
  - `ruff` passed.
  - `pytest unit` passed (23).
  - `pytest integration` passed (13).
  - Docker rebuild + runtime check passed (`compose up -d --build`, `compose ps`, `/api/health`).
  - Runtime smoke: KXO cover via `/api/cover-proxy` now returns `200 image/png`.
### 07. Docs Garble Repair + Commit Prep
- Started `T-049` to fix markdown garbled text and prepare a clean repository commit.
- Rebuilt session context from required memory files before edits.
- Repaired broken `WORKLOG.md` section entries that had placeholder `?` corruption.
- Restored accidentally re-encoded docs and verified markdown files are valid UTF-8.
- Repaired `STATE.md` sample text entries for non-standard latest-chapter examples.
- Confirmed markdown scan has no remaining control-character mojibake or triple-question corruption patterns.
### 08. Round Start: Webhook + RSS Structure Upgrade
- Rebuilt context from required memory files (`PROJECT_BRIEF/REQUIREMENTS/DECISIONS/TASKS/STATE/NEXT_STEP/WORKLOG`).
- Added `T-050` as IN_PROGRESS for breaking-format notification payload upgrade.
- Updated `NEXT_STEP.md` with concrete edit path:
  - webhook rich payload for n8n downstream templating
  - RSS reader-friendly structure for direct consumption
  - tests + docs + Docker verification
### 09. Notification Payload Refactor (Breaking)
- Added `app/services/notification_payloads.py`:
  - unified event enrichment (subscription title/cover/source link + update block)
  - webhook payload composer (`schema_version=2.0`)
- Refactored `app/notifications/webhook.py` to send prebuilt payload body (hash still persisted).
- Refactored `app/services/summary.py` to build enriched events and emit webhook v2 payload.
- Refactored `app/api.py`:
  - debug notify test and webhook test now emit v2 payload format
  - RSS endpoint now renders from enriched events
- Refactored `app/notifications/rss.py`:
  - reader-friendly item title/description text
  - payload hash extractor supports nested v2 event shape
### 10. Regression Coverage
- Added/updated tests:
  - `platform/tests/unit/test_summary.py`: verify daily summary webhook uses v2 payload with enriched fields.
  - `platform/tests/integration/test_api_flow.py`: verify RSS output is directly readable text.
- Validation:
  - `ruff check` passed.
  - `pytest` passed (`38 passed`).
### 11. Runtime Verification + Docs Sync
- Docker verification passed:
  - `cd platform; docker compose up -d --build`
  - `cd platform; docker compose ps`
  - `GET /api/health -> {"status":"ok"}`
- Synced docs:
  - `README.md` updated with webhook v2 and reader-friendly RSS contract.
  - `ARCHITECTURE.md` updated with notification payload module and contract.
  - `DECISIONS.md` added `D-031` for breaking payload migration.
### 12. Round Start: RSS Reader UX Polish
- Started `T-051` after user feedback on real RSS reader display quality.
- Scope locked to RSS presentation only:
  - improve layout/readability and field ergonomics
  - keep existing debug inclusion behavior unchanged in this round
### 13. RSS Structure Polish Implementation
- Reworked `app/notifications/rss.py` for reader compatibility:
  - added RSS namespaces (`atom`, `content`, `media`)
  - concise `description` summary line
  - detailed `content:encoded` text block
  - cover published via `media:thumbnail` + `enclosure`
  - kept robust timezone handling and deterministic item metadata fields
- Fixed prior mojibake literals in feed text templates.
### 14. Validation and Runtime Check
- Updated integration expectation for new RSS fields (`content:encoded`, `media:thumbnail`, `enclosure`).
- Validation:
  - `ruff check` passed
  - `pytest` passed (`38 passed`)
- Docker verification:
  - `cd platform; docker compose up -d --build` passed
  - `docker compose ps` service `Up`
  - `/api/health` returned `{"status":"ok"}`
### 15. Docs + Decision Sync
- Updated `README.md` notification section with refined RSS presentation contract.
- Updated `ARCHITECTURE.md` RSS contract details.
- Added `D-032` in `DECISIONS.md` for reader-UX-oriented RSS structure refinement.
### 16. Bugfix Round: CopyManga RSS Wrong Domain
- User reported RSS links for CopyManga pointed to wrong/scam domain.
- Root cause:
  - source URL builder used legacy `copymanga.site`
  - historical events could carry old-domain `update_url`
- Fix:
  - `notification_payloads.py` now builds CopyManga source links with `https://www.mangacopy.com`.
  - Added URL normalizer to rewrite legacy `copymanga.site` links to official domain during payload build.
  - Normalization applies to both `subscription.source_item_url` and `update.update_url`.
- Tests:
  - Added unit tests for URL normalization behavior.
  - Added integration test to assert RSS output has no `copymanga.site`.
  - Full validation passed (`ruff`, `pytest 42 passed`).
- Runtime verification:
  - Docker rebuild/start passed.
  - `/api/health` returned `ok`.
  - Live RSS check confirms `NO_BAD_DOMAIN`.
### 17. Subscription Lifecycle v2 (Unsubscribe/RSS/Summary)
- Started `T-053` and updated `NEXT_STEP.md` before coding.
- Backend:
  - `DELETE /api/subscriptions/{id}` now supports `purge_history` and reports removed event count.
  - Unsubscribe default behavior removes unsummarized events for that subscription.
  - RSS query now joins `subscriptions` and filters `status=active`.
  - Daily summary candidate query now joins `subscriptions` and filters `status=active`.
- Tests:
  - Added integration coverage for unsubscribe default cleanup, purge-history cleanup, and RSS active-only visibility.
  - Updated unit summary tests to include active subscription rows and added non-active exclusion case.
- Validation:
  - `ruff check` passed.
  - `pytest` passed (`46 passed`).
  - Docker verification passed (`compose up -d --build`, `compose ps`, `/api/health`).
