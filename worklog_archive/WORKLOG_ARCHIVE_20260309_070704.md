# Worklog

## Archive Summary
- Archive file: `worklog_archive/WORKLOG_ARCHIVE_20260309_061401.md`
- Archived range: 2026-03-09 rounds `01` to `76`
- Key outcomes:
  - Greenfield `platform/` MVP delivered (backend, frontend, Docker, CI/CD)
  - CopyManga search stability repaired (header contract + upstream error boundary)
  - Search UX enhanced (cover, pagination, metadata text)
  - Metadata enrichment hardened (cache + fetch-status + multi-signal parser)
  - T-014 docs delivered (branch protection + NAS ops runbook)
  - Governance baseline established (Git + GPLv3 + README/.gitignore maintenance)
  - Post-change Docker verification policy established (`D-015`)

## 2026-03-09

### 01. Worklog Rotation Start
- Action: Detected `WORKLOG.md` exceeded threshold (`286` lines > `150`).
- Result: Rotation workflow triggered for archive + new concise worklog.

### 02. Worklog Archived
- Action: Archived prior worklog to `worklog_archive/WORKLOG_ARCHIVE_20260309_061401.md`.
- Result: Full historical execution details preserved in archive file.

### 03. New Worklog Initialized
- Action: Created new concise `WORKLOG.md` with archive summary header and current-round entries.
- Result: Active worklog is compact and ready for next implementation rounds.

### 04. Protocol Rule Update
- Action: Updated `PROMPT_TEMPLATE.md` to require automatic worklog archival when line count exceeds 150.
- Result: Protocol now explicitly defines threshold-based worklog rotation behavior.

### 05. Decision Sync
- Action: Added `D-016` in `DECISIONS.md` for worklog rotation policy.
- Result: Rotation policy is now part of formal decision history.

### 06. Task/State/NEXT_STEP Sync
- Action: Updated `TASKS.md`, `STATE.md`, and `NEXT_STEP.md` to reflect the new protocol and completed rotation.
- Result: Memory and execution ledgers are aligned with current policy.

### 07. Docker Verification (Protocol Required)
- Action: Executed `cd platform && docker compose up -d --build`, then checked `docker compose ps` and `/api/health`.
- Result:
  - Container rebuilt and started successfully.
  - Service status is `Up` on `0.0.0.0:8000`.
  - Health endpoint returned `{"status":"ok"}`.

### 08. README/.gitignore Governance Check
- Action: Reviewed root `README.md` and `.gitignore` for this round.
- Result: No content change required; governance rule satisfied via review.

### 10. JoJo Non-Standard Latest-Name Reproduction
- Action: Queried local `/api/search` with `JoJo的奇妙冒險` and `JoJo的奇妙冒險全彩版`.
- Result: Reproduced issue: target items had `latest_update_time` but empty `latest_chapters`.

### 11. Root-Cause Inspection via Container HTML
- Action: Inspected `https://www.mangacopy.com/comic/jojo` and `.../jojodqmmxqcb` HTML inside running container.
- Result: Confirmed latest names are present in `<title>` (`恶灵的失恋2`, `第7部24卷`) while current extractor path missed them.

### 12. Parser Patch (Title Fallback)
- Action: Updated `platform/backend/app/adapters/copymanga.py` to add high-priority `<title>`-pattern latest-name fallback extraction.
- Result: Non-standard chapter/volume names are now captured when JSON/label patterns are absent.

### 13. Regression Tests Added
- Action: Added unit tests in `platform/tests/unit/test_copymanga_adapter.py` for title-based non-standard latest names.
- Result: Reported cases are now test-covered.

### 14. Validation and Runtime Verification
- Action:
  - Ran backend `ruff check`.
  - Ran adapter unit tests (`pytest -q platform/tests/unit/test_copymanga_adapter.py`).
  - Ran protocol-required Docker rebuild/start (`docker compose up -d --build`), then `docker compose ps` and `/api/health`.
  - Re-queried `/api/search` with both JoJo keywords.
- Result:
  - Lint/tests passed (`9 passed`).
  - Container is healthy (`/api/health` -> `{"status":"ok"}`).
  - Runtime API now returns expected latest names:
    - `JoJo的奇妙冒險` -> `恶灵的失恋2`
    - `JoJo的奇妙冒險全彩版` -> `第7部24卷`

### 15. Ledger Sync (Non-Standard Name Bugfix)
- Action: Updated `DECISIONS.md` (`D-017`), `TASKS.md` (`T-026`), `STATE.md`, and `NEXT_STEP.md`.
- Result: Memory/decision/task state is synchronized with this bugfix delivery.

### 16. README/.gitignore Governance Check
- Action: Reviewed root `README.md` and `.gitignore` for this round.
- Result: No content change required; governance requirement is satisfied via review.

### 17. Over-Capture Bug Reproduction
- Action: Reproduced user-reported over-capture via `/api/search` for:
  - `JoJo的奇妙冒險`
  - `JoJo的奇妙冒險全彩版`
- Result: Confirmed duplicated/noisy latest entries in some items (short+long duplicate chapter names and unrelated sentence noise).

### 18. Parser Refinement for Over-Capture
- Action: Updated `platform/backend/app/adapters/copymanga.py`:
  - added chapter-token extraction for canonical dedupe
  - normalized long variants to concise token display (for example `...漫畫-第110話` -> `第110話`)
  - filtered fallback noise when no valid chapter token is present.
- Result: Non-standard names are retained while duplicated and noisy fallback captures are suppressed.

### 19. Regression Tests for De-Dupe and Noise Filter
- Action: Added unit regression for dedupe/noise case in `platform/tests/unit/test_copymanga_adapter.py`.
- Result: Parser behavior for user-reported over-capture patterns is test-covered.

### 20. Validation + Runtime Verification
- Action:
  - Ran backend `ruff check`.
  - Ran adapter unit tests (`pytest -q platform/tests/unit/test_copymanga_adapter.py`).
  - Ran protocol-required Docker rebuild/start (`docker compose up -d --build`), then `docker compose ps` and `/api/health`.
  - Re-ran live `/api/search` for both JoJo keywords.
- Result:
  - Lint passed.
  - Adapter tests passed (`10 passed`).
  - Container is healthy (`/api/health` -> `{"status":"ok"}`).
  - Runtime output now avoids over-capture:
    - `JoJo的奇妙冒險 第9部 The JOJO Lands` -> `第27话`
    - `JOJO的奇妙冒險第8部乔乔福音` -> `第110話`

### 21. Ledger Sync (Over-Capture Bugfix)
- Action: Updated `DECISIONS.md`, `TASKS.md`, `STATE.md`, and `NEXT_STEP.md`.
- Result: Memory/decision/task ledgers are synchronized with this refinement round.

### 22. README/.gitignore Governance Check
- Action: Reviewed root `README.md` and `.gitignore` for this round.
- Result: No content change required; governance maintenance rule is satisfied.

### 23. Protocol Exception Update
- Action: Updated `PROMPT_TEMPLATE.md` to add:
  - explanation-only rounds may skip memory-file updates
  - docs-only rounds may skip Docker build/start verification
- Result: Workflow now has explicit efficiency exceptions while keeping default strict validation.

### 24. Decision Sync (Protocol Exceptions)
- Action: Added `D-019` to `DECISIONS.md` to formalize memory/docker exception boundaries.
- Result: Protocol exception behavior is now traceable in decision history.

### 25. Docs-Only Round Validation Policy
- Action: Classified this round as docs/protocol-only (no program-body file touched) and skipped Docker build according to new rule.
- Result: Execution follows updated protocol without unnecessary container rebuild.

### 26. Ledger Sync (Protocol Exception Round)
- Action: Updated `NEXT_STEP.md`, `TASKS.md`, and `STATE.md` for protocol change completion.
- Result: Memory/task/state ledgers are consistent with the new exception policy.

### 09. Non-Standard Latest-Chapter Bugfix Start
- Action: Updated NEXT_STEP.md for reported JoJo non-standard latest-chapter extraction failures.
- Result: Current round scope is locked to adapter parsing fix + regression tests + Docker verification.

### 27. Round Start (Timezone + Debug Tools)
- Action: Rebuilt session context from all memory files and rewrote `NEXT_STEP.md` with this round scope.
- Result: Task scope locked to IP timezone auto-detection, settings guide text, subscription debug actions, and notification trigger verification.

### 28. Backend: Timezone Auto + Debug API
- Action: Added `timezone_auto` config/schema/runtime setting, IP timezone lookup service, and API auto-apply flow on settings/schedules reads.
- Action: Added debug endpoints:
  - `POST /api/subscriptions/{id}/debug/notify-test`
  - `POST /api/subscriptions/{id}/debug/simulate-update`
- Action: Updated summary candidate query to exclude `dedupe_key` with `debug:` prefix.
- Result: Supports requested debug operations while preventing simulated events from triggering automatic daily summary notifications.

### 29. Frontend: UX and Debug Controls
- Action: Updated `main.js` and `style.css`:
  - added `Timezone Auto (by IP)` toggle and timezone input disable logic
  - added light-style guidance text under `Schedules & Settings`
  - added subscription-row debug buttons (`Sim Update`, `Test Notify`)
- Result: Configuration readability improved and debug actions are directly available in subscriptions UI.

### 30. Regression Tests
- Action: Added/updated tests:
  - `test_daily_summary_ignores_debug_events`
  - integration tests for debug endpoints and auto-timezone setting behavior
- Result: Behavior is covered for debug-event isolation, channel reporting, and auto-timezone response path.

### 31. Verification
- Action:
  - ran `ruff check` (pass)
  - ran backend tests (pass: `18 passed`)
  - ran frontend `npm run lint/test/build` via `cmd` (pass)
  - rebuilt and started Docker stack twice for final-state verification
  - checked `docker compose ps` and `GET /api/health` (healthy)
  - smoke-tested new endpoints on running container (notify-test/simulate-update + summary no_updates)
- Result: End-to-end validation passed; debug simulated events do not trigger summary pushes.

### 32. Ledger and Governance Sync
- Action: Updated `REQUIREMENTS.md`, `DECISIONS.md` (`D-020`), `TASKS.md` (`T-029`), `STATE.md`, and `NEXT_STEP.md`.
- Action: Reviewed root `README.md` and `.gitignore` for governance compliance.
- Result: Memory ledgers are synchronized with this round and repository governance requirement is satisfied.
