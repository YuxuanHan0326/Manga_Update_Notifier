# Worklog

## 2026-03-09

### 01. Memory Rebuild
- Action: Read available memory files and checked required set.
- Result: `DECISIONS.md`, `NEXT_STEP.md`, `WORKLOG.md` were missing; other core memory files present.

### 02. Decision Baseline
- Action: Created `DECISIONS.md` with initial accepted decisions (scope, deployment, memory protocol).
- Result: Decision history is now explicit and versionable.

### 03. Next-Step Handoff
- Action: Created `NEXT_STEP.md` with task focus, blockers, next concrete edits, and hard constraints.
- Result: Short-term implementation handoff is now explicit.

### 04. Task-State Alignment
- Action: Updated `TASKS.md` to mark decision-log bootstrap done and added recurring short-term memory maintenance task (`T-012`).
- Result: Task tracker now matches actual project status.

### 05. State Synchronization
- Action: Updated `STATE.md` to include new memory documents and revised next-step list.
- Result: Session handoff state is consistent with current repository reality.

### 06. Protocol Compliance Verification
- Action: Re-read the mandatory memory set (`PROJECT_BRIEF.md`, `REQUIREMENTS.md`, `DECISIONS.md`, `TASKS.md`, `STATE.md`, `NEXT_STEP.md`) after creating missing files.
- Result: The required pre-implementation context rebuild workflow is now executable end-to-end.

### 07. Next-Step Refresh
- Action: Updated `NEXT_STEP.md` to shift focus to `ARCHITECTURE.md` v0.1 as the immediate delivery target.
- Result: Upcoming implementation target is explicit and actionable.

### 08. Task Ledger Update
- Action: Updated `TASKS.md` to record protocol activation completion (`T-013`).
- Result: Deliverable tracking reflects current completion state.

### 09. State Ledger Update
- Action: Updated `STATE.md` to include memory read-cycle verification.
- Result: Project state now records that protocol compliance is active.

### 10. Implementation Kickoff Preparation
- Action: Re-read required memory files and updated `NEXT_STEP.md` before coding work.
- Result: Current round is aligned with implementation plan v2 and protocol constraints.

### 11. Project Skeleton Creation
- Action: Created new `platform/` implementation workspace with backend, frontend, tests, and workflow directories.
- Result: Greenfield structure is ready without modifying reference repositories.

### 12. Backend MVP Build
- Action: Implemented FastAPI app, scheduler manager, adapter abstraction, CopyManga adapter, SQLite models, services, notifications, and API routes.
- Result: Core Phase 1 backend flow is available (`search -> subscribe -> check -> events -> daily summary -> webhook/rss`).

### 13. Frontend MVP Build
- Action: Implemented Vite-based frontend with search, one-click subscribe, subscription list, schedules/settings, and events views.
- Result: Minimal Web UI is present and build/lint/test scripts are available.

### 14. CI/CD Scaffolding
- Action: Added root `Makefile` CI entrypoints and GitHub workflows (`ci.yml`, `release.yml`, `security.yml`), plus Docker artifacts.
- Result: CI/CD split strategy is encoded in repo configuration.

### 15. Validation Pass
- Action: Installed backend dependencies and executed `ruff`, unit tests, and integration tests.
- Result: Backend lint/tests are passing after fixes (encoding, scheduler toggle in tests, sqlite lock handling, line-length cleanup).

### 16. Frontend Validation
- Action: Installed frontend dependencies with npm and ran lint/test/build.
- Result: Frontend lint/tests/build all passed.

### 17. Docker Smoke Attempt
- Action: Attempted local Docker build validation.
- Result: Blocked by unavailable local Docker daemon (`dockerDesktopLinuxEngine` pipe missing); follow-up task recorded.

### 18. Documentation and Tracking Sync
- Action: Added `ARCHITECTURE.md`, updated `DECISIONS.md`, `TASKS.md`, `STATE.md`, and `NEXT_STEP.md` to match implementation state.
- Result: Memory and task ledgers now reflect delivered MVP and remaining hardening items.

### 19. Final Verification and Cleanup
- Action: Re-ran backend lint/tests (`ruff`, unit, integration) and removed generated local artifacts (`node_modules`, `dist`, `__pycache__`, temporary SQLite files).
- Result: Workspace remains clean while preserving implementation deliverables.

### 20. Docker Validation Round Start
- Action: Rebuilt memory context and refreshed `NEXT_STEP.md` to execute live Docker verification.
- Result: Verification sequence is now explicit (`build -> compose up -> api smoke -> state sync`).

### 21. Docker Build + Compose Validation
- Action: Ran elevated Docker validation (`docker version`, `docker build`, `docker compose up -d --build`, `docker compose ps`).
- Result: Image build succeeded and service started successfully on `0.0.0.0:8000`.

### 22. Runtime API Smoke Validation
- Action: Executed live API checks against running container:
  - `/api/health`
  - `/api/sources`
  - `/api/settings`
  - `/api/notifications/rss.xml`
  - subscription create/delete
  - `run-check`
  - `run-daily-summary`
- Result: All tested endpoints returned successful responses; scheduler/check path remained resilient under adapter-side failure.

### 23. Post-Validation Log Review
- Action: Reviewed `docker compose logs --tail=200`.
- Result: Service startup healthy; invalid demo subscription triggered adapter JSON parse error but did not crash process (NFR-001 behavior preserved).

### 24. Bugfix Round Start (Search 500)
- Action: Updated `NEXT_STEP.md` for search internal-server-error fix and targeted self-check scope.
- Result: Current round is scoped to minimal-change bugfix + regression verification.

### 25. NEXT_STEP Sync and Root-Cause Lock
- Action: Refined `NEXT_STEP.md` with concrete edit targets and confirmed root cause (`JSONDecodeError` from non-JSON upstream search payload).
- Result: Edit scope fixed to `copymanga.py::search`, `api.py::search`, and regression coverage.

### 26. Minimal Backend Bugfix
- Action: Implemented adapter boundary guards:
  - Added adapter exception types in `app/adapters/base.py`.
  - Hardened CopyManga response handling in `app/adapters/copymanga.py` for HTTP errors and non-JSON/malformed payloads.
  - Added API boundary handling in `app/api.py` so `/api/search` returns controlled `502` on adapter upstream failures.
- Result: Search path no longer throws unhandled 500 on malformed upstream content.

### 27. Regression Test Addition
- Action: Added integration regression test `test_api_search_returns_502_on_adapter_upstream_error` in `platform/tests/integration/test_api_flow.py`.
- Result: Failure mode is now covered by automated tests.

### 28. Automated Self-Check
- Action: Ran backend lint + tests:
  - `ruff check platform/backend/app platform/tests`
  - `pytest -q platform/tests/unit platform/tests/integration`
- Result: All checks passed (`4 passed`), no lint violations.

### 29. Runtime Validation Self-Check
- Action: Rebuilt and restarted service via Docker, then executed runtime API checks (`health`, `sources`, `settings`, `subscriptions`, `events`, `rss`, `run-check`, `run-daily-summary`, and `search` failure-path probe).
- Result:
  - `/api/search` now returns `502 Bad Gateway` with upstream error detail instead of `500 Internal Server Error`.
  - No unhandled traceback observed in service logs for the search failure path.

### 30. Ledger Sync
- Action: Updated `TASKS.md`, `STATE.md`, and `NEXT_STEP.md` to reflect completion of search 500 fix and define post-fix handoff.
- Result: Execution memory and task/state tracking now match current implementation and validation outcome.

### 31. Decision Log Update
- Action: Added `D-009` in `DECISIONS.md` to formalize `/api/search` upstream failure boundary (`502` instead of unhandled `500`).
- Result: New error-handling contract is explicitly documented for future rounds.

### 32. Bugfix Round Start (Search Still Unusable)
- Action: Updated NEXT_STEP.md to target CopyManga search recovery using reference implementation strategy from copymanga-downloader.
- Result: Current round scope locked to minimal adapter-level compatibility fix + validation.

### 33. Root Cause Validation Against Reference Search Client
- Action: Compared with `copymanga-downloader` (`src-tauri/src/copy_client.rs`) and ran live probes inside container.
- Result: Verified missing request headers (`version`, `webp`, `region`) cause CopyManga to return `200 text/html` with `error`; adding these headers yields normal JSON responses.

### 34. Minimal Compatibility Fix
- Action: Updated `CopyMangaAdapter._headers()` to include `version=2025.08.15`, `webp=1`, `region=1` to match proven request shape.
- Result: Search request format now aligns with working reference implementation while preserving existing adapter/API boundaries.

### 35. Regression Test for Header Contract
- Action: Added `platform/tests/unit/test_copymanga_adapter.py` to assert search call includes required CopyManga headers and still parses response payload.
- Result: Prevents future regressions that would re-trigger HTML `error` responses on search.

### 36. Build/Runtime Verification Unblock
- Action: Docker rebuild initially failed because local `platform/frontend/node_modules` polluted build context; added `platform/.dockerignore` to exclude frontend local artifacts.
- Result: Docker image build and compose startup recovered for this workspace.

### 37. Validation Round (Post-Fix)
- Action:
  - Ran `ruff check platform/backend/app platform/tests`.
  - Ran `pytest -q platform/tests/unit platform/tests/integration`.
  - Rebuilt container and executed runtime API smoke checks including `/api/search`.
  - Reviewed `docker compose logs --tail=80`.
- Result:
  - Lint/tests passed (`5 passed`).
  - `/api/search?source=copymanga&q=test&page=1` returns `200` with JSON payload.
  - No unhandled runtime exception observed in logs.

### 38. Ledger Sync
- Action: Updated `TASKS.md`, `STATE.md`, and `NEXT_STEP.md` for this round's search usability fix and current handoff.
- Result: Task/state memory reflects real runtime status and follow-up priorities.

### 39. Decision Sync
- Action: Added `D-010` in `DECISIONS.md` for CopyManga header contract used by search/update APIs.
- Result: New compatibility requirement is explicit and traceable.

### 40. Search API Contract Sanity Check
- Action: Verified non-happy-path behavior after fix:
  - unknown source returns `404`
  - empty query returns validation `422`
- Result: Search success-path fix did not regress API contract boundaries.

### 41. Requirement Change Round Start (Search UX Enhancement)
- Action: Updated NEXT_STEP.md for new requirements: cover display, pagination, and latest update metadata from webpage scraping.
- Result: Implementation scope is locked to search adapter + search UI minimal changes.

### 42. Backend Search Metadata Enrichment
- Action: Added webpage metadata extraction in CopyManga adapter to enrich search items with latest update date and latest chapter candidates.
- Result: Search response can now carry latest_update_time and latest_chapters in item meta without breaking existing API shape.

### 43. Frontend Search UX Update
- Action: Updated search table rendering to show cover, author, and light-color metadata; added pager controls (Prev/Next + page info).
- Result: Search results are visually compact, include cover and metadata, and support page navigation.

### 44. Validation Round (Requirement Change)
- Action:
  - Ran `ruff check platform/backend/app platform/tests`.
  - Ran `pytest -q platform/tests/unit platform/tests/integration`.
  - Ran frontend `pnpm install/lint/test/build`.
- Result:
  - Backend checks passed (`7 passed`).
  - Frontend lint/tests/build passed.

### 45. Runtime Search Verification
- Action: Rebuilt Docker image and verified search APIs:
  - `/api/search?source=copymanga&q=1&page=1`
  - `/api/search?source=copymanga&q=1&page=2`
  - `docker compose logs --tail=120`
- Result:
  - Search supports page navigation with stable totals.
  - Response includes `cover` and metadata fields (`latest_update_time`, `latest_chapters`) in item payloads.
  - No unhandled runtime exception observed.

### 46. Ledger Sync
- Action: Updated `TASKS.md`, `STATE.md`, and `NEXT_STEP.md` for requirement-change delivery and handoff.
- Result: Memory/task ledgers now match code and runtime behavior.

### 47. Decision Sync
- Action: Added `D-011` to `DECISIONS.md` for metadata source strategy (webpage scraping + graceful fallback under anti-bot conditions).
- Result: New behavior and tradeoff are documented for future rounds.

### 48. Encoding-Safe Cleanup
- Action: Replaced non-ASCII-sensitive chapter regex literals with Unicode escape forms and normalized frontend metadata labels to ASCII-safe text.
- Result: Parser behavior kept intact while reducing platform encoding issues in tooling/terminals.

### 49. Final Validation Pass
- Action:
  - Re-ran backend `ruff` + `pytest` (`7 passed`).
  - Re-ran frontend `pnpm lint/test/build`.
  - Rebuilt Docker and re-verified `/api/search` page 1/2 and runtime logs.
- Result: Final implementation validated end-to-end on current local Docker runtime.

### 50. Test Fixture Encoding Hardening
- Action: Rewrote new adapter unit test sample text to Unicode-escape form and reran backend checks.
- Result: Tests remain green and are resilient to terminal/codepage differences.

### 51. Governance Round Start
- Action: Rebuilt memory context and switched NEXT_STEP.md to repository governance setup (Git + GPLv3 + README/.gitignore protocol).
- Result: Scope is locked to repository metadata and process-document updates.

### 52. Repository Bootstrap
- Action: Initialized root Git repository and downloaded official GPLv3 text from https://www.gnu.org/licenses/gpl-3.0.txt into root LICENSE.
- Result: Project now has VCS baseline and explicit GPLv3 licensing.

### 53. README and .gitignore Baseline
- Action: Added root README.md (Chinese, user-facing operation guide) and root .gitignore (Python/Node/runtime/reference-repo ignore rules).
- Result: Repository now has explicit onboarding docs and consistent ignore strategy at root level.

### 54. Protocol Placement
- Action: Rewrote PROMPT_TEMPLATE.md in proper Chinese and added mandatory rule to maintain README.md and .gitignore every round.
- Result: New collaboration protocol is explicit and reusable in future sessions.

### 55. Ledger and Decision Sync
- Action: Added D-012 (repository governance baseline), updated TASKS.md, STATE.md, and NEXT_STEP.md for handoff.
- Result: Memory and decision trail now reflect Git/license/document governance requirements.

### 56. Git Ownership Compatibility
- Action: Encountered Windows dubious ownership after git init; added repository path to global Git safe.directory to restore normal Git command usage.
- Result: git status now works for this repository.

### 57. Follow-up Round Start
- Action: Updated NEXT_STEP.md to target requested follow-up tasks (adapter cache, missing-copy distinction, and T-014 docs).
- Result: Current round scope is explicit and aligned with user instruction.

### 58. Execution Kickoff
- Action: Re-read all memory files and refreshed NEXT_STEP.md with in-progress execution details before code edits.
- Result: Round now has explicit ordered actions (backend cache -> frontend copy -> T-014 docs -> README/.gitignore sync).

### 59. Adapter Cache and Meta Status
- Action: Updated `platform/backend/app/adapters/copymanga.py` to add per-`item_id` in-memory TTL cache for webpage metadata and introduce `meta_fetch_status` (`ok`/`fetch_failed`).
- Result: Repeated metadata fetches are deduplicated in short window; frontend can distinguish fetch failure from empty chapter data.

### 60. Adapter Regression Tests
- Action: Extended `platform/tests/unit/test_copymanga_adapter.py` with tests for cache reuse and fetch-failure status behavior.
- Result: New contract is now unit-test covered.

### 61. Frontend Metadata Copy Refinement
- Action: Updated `platform/frontend/src/main.js` `buildSearchMeta()` logic to render `未抓到` vs `暂无章节` based on `meta_fetch_status` and chapter presence.
- Result: Search result metadata text now matches clarified UX requirement.

### 62. T-014 Documentation Delivery
- Action: Added `docs/branch-protection.md` and `docs/nas-ops-runbook.md`; rewrote root `README.md` in accurate Chinese and linked new docs; reviewed root `.gitignore` and kept rules unchanged.
- Result: Branch protection setup and NAS operations guidance are now documented and user-discoverable, and README/.gitignore governance rule is satisfied.

### 63. Validation Round
- Action:
  - Ran `ruff check platform/backend/app platform/tests`.
  - Ran `pytest -q platform/tests/unit/test_copymanga_adapter.py`.
  - Ran frontend `corepack pnpm lint`.
  - Ran frontend `corepack pnpm build` (required escalated execution after sandbox `EPERM`).
- Result:
  - Backend lint/tests passed.
  - Frontend lint/build passed.
  - One sandbox-related build failure (`spawn EPERM`) was resolved via approved escalated run.

### 64. Ledger Sync
- Action: Updated `DECISIONS.md`, `TASKS.md`, `STATE.md`, and `NEXT_STEP.md` for this completion round.
- Result: Memory/decision/task ledgers are synchronized with implemented changes.

### 65. Unknown Last-Update Bugfix Start
- Action: Rebuilt session context from memory files and updated NEXT_STEP.md for new bugfix scope (robust web metadata extraction).
- Result: Round scope is now locked to adapter parsing hardening + regression tests with minimal change surface.

### 66. Adapter Metadata Extraction Hardening
- Action: Reworked CopyManga webpage metadata parser to use layered extraction:
  - JSON embedded fields (`datetime_updated`, `last_chapter` variants)
  - label/value HTML patterns for update time and latest chapter
  - fallback chapter-name patterns.
- Result: Parser no longer depends on one narrow HTML class pair and can capture non-standard latest chapter names.

### 67. Escaped Payload Compatibility
- Action: Added normalization handling for escaped unicode payloads (for example `\\uXXXX`) in extracted metadata text.
- Result: Latest chapter names in escaped JSON snippets are decoded into readable text instead of remaining escaped literals.

### 68. Regression Tests and Validation
- Action:
  - Added unit tests for JSON-embedded metadata and non-standard chapter names.
  - Added unit test for escaped-json chapter-name decoding.
  - Ran `ruff check platform/backend/app platform/tests`.
  - Ran `pytest -q platform/tests/unit/test_copymanga_adapter.py`.
- Result:
  - Backend lint passed.
  - Adapter unit tests passed (`7 passed`).

### 69. Ledger Sync (Bugfix Round)
- Action: Updated NEXT_STEP.md for completion status and upcoming handoff actions.
- Result: Short-term execution memory reflects current completed bugfix round.

### 70. README/.gitignore Governance Check
- Action: Updated root README to reflect metadata parsing robustness improvement and reviewed root `.gitignore` rules.
- Result: Governance rule for per-round README/.gitignore maintenance is satisfied (README updated, .gitignore no change required).

### 71. Protocol-Update Round Start
- Action: Re-read memory files and updated NEXT_STEP.md for new request: enforce Docker build+startup after every change round and execute it immediately.
- Result: Current round execution scope is explicitly locked to protocol update + runtime Docker verification.

### 72. Docker Build and Startup Verification (Immediate)
- Action: Ran `cd platform && docker compose up -d --build`, then checked `docker compose ps` and `/api/health`.
- Result:
  - Stack rebuilt and container started successfully.
  - Runtime status shows service `Up` with `0.0.0.0:8000->8000/tcp`.
  - Health endpoint returned `{"status":"ok"}`.

### 73. Protocol Document Update
- Action: Rewrote `PROMPT_TEMPLATE.md` in clean Chinese and added mandatory post-change Docker verification rule.
- Result: Team protocol now explicitly requires build+startup verification after every change round.

### 74. Decision Sync (Protocol Rule)
- Action: Added decision `D-015` in `DECISIONS.md` to formalize post-change Docker verification as mandatory delivery step.
- Result: Execution policy is now traceable in decision ledger, not only in prompt template.

### 75. README/.gitignore Governance Check
- Action: Reviewed root `README.md` and root `.gitignore` for this round.
- Result: No content change required; governance maintenance requirement is satisfied via review.

### 76. Ledger Sync (Protocol Round)
- Action: Updated `NEXT_STEP.md`, `TASKS.md`, and `STATE.md` to reflect protocol update and completed Docker verification run.
- Result: Memory/task/state ledgers are aligned with current execution state.
