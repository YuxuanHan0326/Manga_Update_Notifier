# Worklog

## Archive Summary
- Archive file: $dst
- Reason: active WORKLOG.md exceeded 150 lines and was rotated per protocol.
- Key outcomes carried forward:
  - Security workflow repair completed (python-audit/frontend-audit/trivy-image split + trivy container scan path + FastAPI baseline upgrade).
  - Schedule UX round completed: friendly schedule controls (N-hour check + HH:MM daily summary) with advanced cron compatibility.
  - Validation passed: backend (uff, pytest 24 passed) and frontend (lint/test/build).
  - Docker runtime verification recovered and passed (compose up -d --build, compose ps, /api/health).

## 2026-03-11

### 05. KXO 双路径实现启动
- 已按协议重读 `PROJECT_BRIEF.md` / `REQUIREMENTS.md` / `DECISIONS.md` / `TASKS.md` / `STATE.md` / `NEXT_STEP.md`。
- 已更新 `NEXT_STEP.md`：切换为 KXO 双路径实现任务，明确本轮改动范围、约束和验证步骤。
- 已完成代码入口勘查（adapter registry、api、checker、settings、frontend 单页入口），开始实施 KXO 后端与前端改动。

### 06. KXO 后端主线落地
- 新增 `platform/backend/app/adapters/kxo.py`：
  - guest 更新检测：`detail -> token(h) -> book_data` 同会话链路
  - cookie 搜索：认证缺失/失效分别抛出 `auth_required` / `session_invalid`
  - 主备域名回退：`configured -> kzo.moe -> kxo.moe`
- 注册内置 adapter：`register_builtin_adapters()` 增加 `KxoAdapter`。
- 扩展设置模型与 API：
  - 新增 KXO 设置字段（base/auth/cookie/remember/user-agent）
  - 新增 `POST /api/subscriptions/manual-kxo`
  - 新增 `POST /api/settings/kxo/test`
  - 默认 cookie 非持久化（进程内覆盖），仅 `remember_session=true` 时持久化。
- checker 增加运行时配置注入，保证 KXO adapter 在定时任务中读取最新设置。

### 07. 前端与测试
- 前端 `main.js` 增加：
  - KXO source 选择、KXO 设置区、连接测试按钮
  - KXO 手动 URL/ID 订阅入口
  - KXO 搜索提示与 cookie 配置状态提示
- 前端样式调整：输入布局和 textarea 可用性优化。
- 新增/更新测试：
  - `platform/tests/unit/test_kxo_adapter.py`
  - `platform/tests/integration/test_api_flow.py`（KXO 手动订阅、认证错误、配置测试）
- 验证结果：
  - `ruff` 通过
  - `pytest` 通过（`31 passed`）
  - `frontend lint/test/build` 通过

### 08. Docker 运行验证与文档同步
- 执行 `cd platform && docker compose up -d --build`，镜像重建并容器启动成功。
- 执行 `docker compose ps`，`manga-update-platform` 状态为 `Up`。
- 健康检查通过：`GET /api/health` 返回 `200 {"status":"ok"}`。
- 额外冒烟：
  - `GET /api/settings` 返回新增 KXO 字段（`kxo_*`）；
  - `GET /api/search?source=kxo...` 在未配置 cookie 时返回 `401`（符合预期）。
- 已同步更新文档与治理文件：`README.md`、`ARCHITECTURE.md`、`DECISIONS.md`、`TASKS.md`、`STATE.md`、`NEXT_STEP.md`、`.gitignore`、`platform/.env.example`。

### 09. 需求/简报对齐文档轮次（仅文档）
- 按已批准计划更新了：
  - `PROJECT_BRIEF.md`：将当前目标与范围对齐为 CopyManga + KXO（双路径）
  - `REQUIREMENTS.md`：补充 KXO 路径、认证错误语义、手动 URL/ID 订阅与 cookie 持久化规则
- 同步更新台账：
  - `TASKS.md`：T-042 完成
  - `NEXT_STEP.md`、`STATE.md`：补充本轮文档对齐结果
- 已审阅 `README.md` 与 `.gitignore`，本轮无需改动。
- 本轮为 docs-only，按协议跳过 Docker build/启动验证。

### 01. Schedule UX Round Completed
- Added user-friendly schedule controls in frontend settings:
  - check every N hours
  - daily summary time (`HH:MM`)
- Kept advanced cron fields for compatibility with custom schedules.

### 02. Validation
- Backend checks passed: `ruff`, `pytest 24 passed`.
- Frontend checks passed: `npm run lint/test/build`.
- Docker verification passed: `compose up -d --build`, `compose ps`, `/api/health`.

### 03. Ledger Sync
- Updated `README.md`, `ARCHITECTURE.md`, `DECISIONS.md` (`D-026`), `TASKS.md`, `NEXT_STEP.md`, `STATE.md`.
- Worklog rotated per threshold and archived to `worklog_archive/WORKLOG_ARCHIVE_20260311_024526.md`.

### 04. Final UX Polish + Re-Verification
- Localized schedule labels in settings UI for clearer Chinese UX.
- Re-ran frontend validation (`npm run lint/test/build`) after label update.
- Re-ran Docker verification after final frontend edit:
  - `docker compose up -d --build`
  - `docker compose ps`
  - `GET /api/health`
- Result: all checks passed.

### 10. 前端分页/分区改造（General / CopyManga / KXO）
- 按协议先更新台账并设置 `TASKS.md` 的 `T-043` 为 `IN_PROGRESS`，随后实施最小改动前端重构。
- 改造 `platform/frontend/src/main.js`：
  - 新增 tab 导航与分区渲染（`General` / `CopyManga` / `KXO`）
  - 将单一搜索入口拆分为源独立搜索面板（保持原后端 `/api/search` 契约不变）
  - 将 KXO 手动 URL/ID 添加与 KXO 设置迁移到 KXO 分区
  - 保留订阅、事件、调试按钮、调度设置等既有行为
- 改造 `platform/frontend/src/style.css`：
  - 增加 tab 样式与面板显隐规则
  - 保持原有卡片、表格、搜索元信息样式兼容
- 同步文档：
  - `README.md` 补充三分区 UI 说明
  - `ARCHITECTURE.md` 增加前端信息架构章节
- 验证结果：
  - 前端：`npm run lint`、`npm run test`、`npm run build` 全部通过
  - Docker：`cd platform && docker compose up -d --build` 成功
  - 运行态：`docker compose ps` 为 `Up`，`GET /api/health` 返回 `{\"status\":\"ok\"}`

### 11. Bugfix：Schedule 勾选框错位 + KXO 账号密码手动登录入口
- 按协议完成上下文重建后，先创建 `T-044` 为 `IN_PROGRESS` 并更新 `NEXT_STEP.md`。
- 修复 Schedule 区域 tick 错位：
  - 在 `platform/frontend/src/style.css` 增加 `input[type=\"checkbox\"]` 专用样式，避免继承文本输入框 `width: 100%`
  - 新增 `.toggle-option` 布局类，确保勾选框与文案同一行对齐
- 新增 KXO 账号密码手动登录（不存密码）：
  - 后端新增 `POST /api/settings/kxo/login`
  - `KxoAdapter` 新增凭证登录方法，成功后返回会话 Cookie
  - 密码仅用于单次登录请求，不入库、不写配置、不回显
  - Cookie 持久化仍由 `remember_session` 控制
  - 前端 KXO Settings 新增账号/密码输入与 `Credential Login` 按钮
- 回归验证：
  - 后端：`ruff check` 通过
  - 测试：`pytest -q platform/tests/unit platform/tests/integration` 通过（`32 passed`）
  - 前端：`npm run lint`、`npm run test`、`npm run build` 通过
  - Docker：`docker compose up -d --build` + `docker compose ps` + `/api/health` 通过

### 12. 需求变更：KXO 回退为 manual-only + 订阅列表封面展示
- 已按协议先更新 `TASKS.md`（`T-045` IN_PROGRESS）和 `NEXT_STEP.md`，再进入代码改动。
- 后端改动：
  - 移除 KXO 凭证登录接口（`POST /api/settings/kxo/login`）及对应 schema。
  - `GET /api/search?source=kxo` 改为返回可读的 manual-only 提示（400），避免继续走会话链路。
  - `POST /api/subscriptions/manual-kxo` 在订阅 `item_meta` 中写入 `cover` 便于列表展示。
- 前端改动：
  - KXO 页移除搜索与账号密码登录入口，仅保留手动 URL/ID 添加与基础连通设置。
  - 订阅列表新增 `Cover` 列，支持封面渲染与缺省占位。
  - CopyManga 一键订阅时同步写入 `item_meta.cover`，新订阅可立即显示封面。
- 文档与治理同步：
  - 更新 `README.md`、`ARCHITECTURE.md`、`DECISIONS.md`（新增 D-029）。
  - 按需求变更同步更新 `REQUIREMENTS.md`（v0.4）与 `PROJECT_BRIEF.md`。
- 验证结果：
  - 后端：`ruff check` 通过；`pytest` 通过（`30 passed`）
  - 前端：`npm run lint/test/build` 通过
  - Docker：`docker compose up -d --build` 成功，`docker compose ps` 为 Up，`/api/health` 返回 ok
### 13. Bugfix 启动：No Cover 回归排查
- 已按协议重读 `PROMPT_TEMPLATE.md`、`PROJECT_BRIEF.md`、`REQUIREMENTS.md`、`ARCHITECTURE.md`、`DECISIONS.md`、`TASKS.md`、`STATE.md`、`NEXT_STEP.md`、`WORKLOG.md`。
- 已在 `TASKS.md` 创建 `T-046` 为 `IN_PROGRESS`。
- 已更新 `NEXT_STEP.md`，明确本轮目标为修复 CopyManga/KXO 订阅封面缺失。
- 排查结论（待修复）：历史订阅缺少 `item_meta.cover`，且 KXO 封面提取仅依赖 `og:image`，覆盖不足。
### 14. Bugfix 实施：订阅封面回填 + 抓取增强
- 代码改动：
  - `platform/backend/app/adapters/kxo.py`：增强 `_extract_cover`，新增多信号封面提取（meta/json/img）与 URL 归一化。
  - `platform/backend/app/adapters/copymanga.py`：`_extract_web_meta` 增加封面提取，新增 `fetch_item_snapshot` 供订阅回填复用。
  - `platform/backend/app/services/subscriptions.py`：新增 `backfill_subscription_covers`，对历史缺失封面记录做读取时惰性回填并落库。
  - `platform/backend/app/api.py`：`GET /api/subscriptions` 接入封面回填流程。
- 测试改动：
  - `platform/tests/unit/test_kxo_adapter.py` 新增 KXO 封面提取回归。
  - `platform/tests/unit/test_copymanga_adapter.py` 新增 CopyManga 封面提取回归。
  - `platform/tests/integration/test_api_flow.py` 新增历史订阅封面自动回填集成测试。
- 验证结果：
  - `ruff check backend tests` 通过。
  - `pytest -q platform/tests/unit` 通过（23 passed）。
  - `pytest -q platform/tests/integration` 通过（10 passed）。
  - 前端：`npm run lint` 通过；`npm run test/build` 在沙箱下 `spawn EPERM`，提权执行后通过。
  - Docker：`cd platform && docker compose up -d --build`、`docker compose ps`、`/api/health` 均通过。
- 文档同步：`README.md` 已补充“历史无封面订阅读取时自动回填”说明。
